NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR-ANON-KEY"

# Server-only secrets (do NOT expose)
# Required in production
SESSION_SECRET="change-me-to-32+chars-random"
# Optional: enable server-side writes (posting/moderation)
SUPABASE_SERVICE_ROLE_KEY="service-role-key-here"
SUPABASE_PROJECT_REF="your-project-ref"
IP_HASH_SALT="random-32+chars-for-ip-hash"
# Demo tokens (only enable in local development)
ENABLE_DEMO_TOKENS="true"
NEXT_PUBLIC_ENABLE_DEMO_TOKENS="true"
